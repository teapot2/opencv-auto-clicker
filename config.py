# THRESHOLD is the minimum similarity score required for a match to be considered valid.
THRESHOLD = 10

# TARGET_IMAGE_PATH is the file path of the image that will be used as the target for matching.
TARGET_IMAGE_PATH = "assets/image.png"

# SLEEP_TIME is the time in seconds to wait between iterations of the main loop.
SLEEP_TIME = 0.1
